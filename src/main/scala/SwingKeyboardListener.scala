/**
 * Created with IntelliJ IDEA.
 * User: Nick
 * Date: 8/19/13
 * Time: 7:02 PM
 * To change this template use File | Settings | File Templates.
 */
class SwingKeyboardListener {
  // http://stackoverflow.com/questions/1248510/convert-string-to-keyevents
  //  val top = new MainFrame(null) {      run
  //    val label = new Label {
  //      text = "waiting..."                                      c
  //    }
  //    contents = new BoxPanel(Orientation.Vertical) {
  //      contents += label
  //      border = Swing.EmptyBorder(0, 0, 0, 0)
  //      listenTo(keys)
  //      reactions += {
  //        case KeyPressed(_, Key.Space, _, _) => label.text = "Space is down"
  //        case KeyReleased(_, Key.Space, _, _) => label.text = "Space is up"
  //      }
  //      focusable = true
  //      requestFocus
  //    }
  //  }


  //  val eventMask = AWTEvent.MOUSE_MOTION_EVENT_MASK + AWTEvent.MOUSE_EVENT_MASK + AWTEvent.KEY_EVENT_MASK;
  //  Toolkit.getDefaultToolkit().addAWTEventListener(new AWTEventListener() {
  //    def eventDispatched(e: AWTEvent) {
  //      System.out.println(e.getID())
  //    }
  //  }, eventMask);
  //
  //   while(true){Thread.sleep(100)}
}
